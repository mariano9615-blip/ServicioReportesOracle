<UserControl x:Class="ServicioReportesOracle.UI.Views.AlertasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             Foreground="{StaticResource OnSurfaceBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="💌 Alertas SMTP Enviadas"
                           FontSize="24" FontWeight="Bold"
                           Foreground="{StaticResource OnSurfaceBrush}"/>
                <TextBlock Text="{Binding TotalAlertas, StringFormat=' ({0} total)'}"
                           FontSize="16" Margin="8,0,0,0"
                           Foreground="{StaticResource OnSurfaceBrush}"
                           VerticalAlignment="Bottom"/>
                <TextBlock Text="{Binding AlertasHoy, StringFormat=' | {0} hoy'}"
                           FontSize="14" Margin="4,0,0,0"
                           Foreground="{StaticResource OnSurfaceBrush}"
                           Opacity="0.7"
                           VerticalAlignment="Bottom"/>
            </StackPanel>

            <Button Grid.Column="1" Content="📊 Exportar a Excel"
                    Command="{Binding ExportarCommand}"
                    Background="{StaticResource SurfaceContainerHighBrush}"
                    Foreground="{StaticResource OnSurfaceBrush}"
                    BorderBrush="{StaticResource OutlineBrush}"
                    BorderThickness="1"
                    Style="{x:Null}"
                    Margin="0,0,8,0" Padding="12,8"/>

            <Button Grid.Column="2" Content="🔄 Actualizar"
                    Command="{Binding ActualizarCommand}"
                    Background="{StaticResource SurfaceContainerHighBrush}"
                    Foreground="{StaticResource OnSurfaceBrush}"
                    BorderBrush="{StaticResource OutlineBrush}"
                    BorderThickness="1"
                    Style="{x:Null}"
                    Padding="12,8"/>
        </Grid>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Alertas}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  ClipboardCopyMode="IncludeHeader"
                  Background="{StaticResource SurfaceBrush}"
                  BorderBrush="{StaticResource OutlineBrush}"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource OutlineBrush}"
                  RowBackground="{StaticResource SurfaceBrush}"
                  AlternationCount="2"
                  AlternatingRowBackground="#0A000000"
                  RowHeight="32"
                  Foreground="{StaticResource OnSurfaceBrush}">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    <Setter Property="BorderBrush" Value="#33FFFFFF"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="12,6"/>
                    <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#4F46E5"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22FFFFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding TimestampFormateado}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoAmigable}" Width="140"/>

                <DataGridTextColumn Header="Asunto" Binding="{Binding Asunto}" MinWidth="250" MaxWidth="400">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding Asunto}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Detalle" Binding="{Binding Detalle}" MinWidth="200" MaxWidth="350">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding Detalle}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="ID Ref." Binding="{Binding IdReferencia}" Visibility="Collapsed"/>

                <DataGridTextColumn Header="Destinatarios" Binding="{Binding DestinatariosStr}" MinWidth="180" MaxWidth="250">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip" Value="{Binding DestinatariosStr}"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Origen" Binding="{Binding Origen}" Visibility="Collapsed"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
