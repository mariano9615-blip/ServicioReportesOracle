<UserControl x:Class="ServicioReportesOracle.UI.Views.SqlEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Editor SQL de Pruebas" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryLightBrush}" Margin="0,0,0,20"/>

        <!-- Query Editor -->
        <TextBox Grid.Row="1" Text="{Binding QueryText}"
                 AcceptsReturn="True"
                 FontFamily="Consolas"
                 FontSize="14"
                 TextWrapping="Wrap"
                 VerticalContentAlignment="Top"
                 Margin="0,0,0,15"/>

        <DockPanel Grid.Row="2" Margin="0,0,0,15">
            <Button Content="⚡ EJECUTAR CONSULTA" Command="{Binding ExecuteCommand}" Background="{StaticResource PrimaryBrush}" Foreground="White" Padding="25,12" FontWeight="Bold">
                 <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
            <TextBlock Text="* Se limita a los primeros 100 resultados" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="{StaticResource OnSurfaceVariantBrush}" Opacity="0.6"/>
        </DockPanel>

        <!-- Results Grid -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
            <DataGrid ItemsSource="{Binding Results}" 
                      AutoGenerateColumns="True" 
                      IsReadOnly="True"
                      ClipboardCopyMode="IncludeHeader"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"/>
        </Border>
    </Grid>
</UserControl>
