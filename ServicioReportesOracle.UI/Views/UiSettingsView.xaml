<UserControl x:Class="ServicioReportesOracle.UI.Views.UiSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700">

    <Grid Margin="15">
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="40,30"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MinWidth="440" MaxWidth="520">
            <StackPanel>
                <!-- Título -->
                <TextBlock Text="⚙️ Configuración de Interfaz" FontSize="22" FontWeight="Bold"
                           FontFamily="Segoe UI Emoji"
                           Foreground="{StaticResource PrimaryLightBrush}" Margin="0,0,0,6"/>
                <TextBlock Text="Ajustes visuales de la consola de gestión."
                           FontSize="12" Foreground="{StaticResource OnSurfaceVariantBrush}" Margin="0,0,0,28"/>

                <Border Height="1" Background="{StaticResource OutlineBrush}" Margin="0,0,0,28"/>

                <!-- Sección: Tamaño de ventana -->
                <TextBlock Text="Tamaño de ventana" FontSize="14" FontWeight="SemiBold"
                           Foreground="{StaticResource OnSurfaceBrush}" Margin="0,0,0,12"/>

                <TextBlock Text="Ancho de la ventana al iniciar:" FontSize="12"
                           Foreground="{StaticResource OnSurfaceVariantBrush}" Margin="0,0,0,8"/>

                <ComboBox ItemsSource="{Binding WidthPercentOptions}"
                          SelectedItem="{Binding SelectedWidthPercent, UpdateSourceTrigger=PropertyChanged}"
                          Height="38" Width="160" HorizontalAlignment="Left"
                          FontSize="14" Margin="0,0,0,10">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#2D2D2D"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton x:Name="ToggleButton"
                                                          Focusable="False"
                                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                          ClickMode="Press">
                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border x:Name="TgBd"
                                                                Background="#2D2D2D"
                                                                CornerRadius="6"
                                                                BorderThickness="0">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="20"/>
                                                                </Grid.ColumnDefinitions>
                                                                <ContentPresenter Grid.Column="0"
                                                                                  Margin="8,4,0,4"
                                                                                  VerticalAlignment="Center"/>
                                                                <Path Grid.Column="1"
                                                                      x:Name="Arrow"
                                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                                      Fill="White"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="TgBd" Property="Background" Value="#3A3A3A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="TgBd" Property="Background" Value="#3A3A3A"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>

                                            <ContentPresenter x:Name="ContentSite"
                                                              IsHitTestVisible="False"
                                                              Content="{TemplateBinding SelectionBoxItem}"
                                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                              Margin="8,4,24,4"
                                                              VerticalAlignment="Center"
                                                              TextElement.Foreground="White"/>

                                            <Popup x:Name="Popup"
                                                   Placement="Bottom"
                                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                                   AllowsTransparency="True"
                                                   Focusable="False"
                                                   PopupAnimation="Slide">
                                                <Grid x:Name="DropDown"
                                                      SnapsToDevicePixels="True"
                                                      MinWidth="{TemplateBinding ActualWidth}"
                                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                    <Border x:Name="DropDownBorder"
                                                            Background="#2D2D2D"
                                                            BorderThickness="1"
                                                            BorderBrush="#444"
                                                            CornerRadius="6"/>
                                                    <ScrollViewer Margin="4,4,4,4" SnapsToDevicePixels="True">
                                                        <StackPanel IsItemsHost="True"
                                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                    </ScrollViewer>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Style>
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#2D2D2D"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="Bd"
                                                Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"
                                                              TextElement.Foreground="White"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#3A3A3A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#0078D4"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="White">
                                <TextBlock.Text>
                                    <Binding StringFormat="{}{0}%"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Se aplica al reiniciar. Usar 'Aplicar ahora' para efecto inmediato."
                           FontSize="11" Foreground="{StaticResource OnSurfaceVariantBrush}"
                           Opacity="0.7" Margin="0,0,0,0"/>

                <Border Height="1" Background="{StaticResource OutlineBrush}" Margin="0,28,0,24"/>

                <!-- Botones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding ApplyNowCommand}"
                            Background="{StaticResource SurfaceContainerHighBrush}" Foreground="White"
                            Padding="20,12" FontWeight="Bold" Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="▶" FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="APLICAR AHORA" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveCommand}"
                            Background="{StaticResource PrimaryBrush}" Foreground="White"
                            Padding="25,12" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" FontFamily="Segoe UI Emoji" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="GUARDAR" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
