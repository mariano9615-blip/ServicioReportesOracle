<UserControl x:Class="ServicioReportesOracle.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Logs del Servicio" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryLightBrush}"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🔄 Actualizar" Command="{Binding RefreshCommand}" Margin="0,0,10,0" Padding="15,5" Background="{StaticResource SurfaceBrush}" Foreground="White">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="🗑 Limpiar Log" Command="{Binding ClearCommand}" Padding="15,5" Background="#33FF0000" Foreground="Red">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="10">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding LogContent}" 
                         IsReadOnly="True" 
                         Background="Transparent" 
                         Foreground="#B0B0B0" 
                         BorderThickness="0" 
                         FontFamily="Consolas" 
                         FontSize="13" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
